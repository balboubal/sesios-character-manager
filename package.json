{
  "name": "sesios-character-manager",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "generate:workbook": "node scripts/generate-workbook-assets.mjs",
    "check": "node scripts/check-project.mjs"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.52.0"
  },
  "devDependencies": {
    "vite": "^7.0.4"
  }
}
